# NPM Configuration for Canvas LMS Dashboard
# This ensures npm uses the correct package-lock.json

# Use local package-lock.json
package-lock=true

# Prevent npm from looking for parent lockfiles
global-style=false

# Use exact versions
save-exact=true
